language: cpp

compiler:
  - gcc

before_script:
  - svn co https://projects.coin-or.org/svn/CoinUtils/stable/2.10/CoinUtils CoinUtils
  - svn co https://projects.coin-or.org/svn/Osi/stable/0.107/Osi Osi
  - ./CoinUtils/configure --prefix=$(TRAVIS_BUILD_DIR) && make install
  - ./Osi/configure --prefix=$(TRAVIS_BUILD_DIR) && make install

os:
  - linux
